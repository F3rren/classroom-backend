cambiare applciation propertirs con le caratteristiche di db e conenssione facendolo in un config.properties
    spirng.config.import=file:./config/config.properties (i file che sono esterni e che possono essere modificati)

buildare il jar con un nome abbastanza esplicativo
cercare come non portare sorgenti nella build finale/come fare una build react con npm

