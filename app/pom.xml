<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.prenotazioni</groupId>
        <artifactId>prenotazioni-aule-parent</artifactId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>

    <artifactId>prenotazioni-aule-backend</artifactId>
    <name>PrenotazioniAuleBackend</name>
    <description>Applicazione: aule, prenotazioni, utenti e notifiche (ancora un solo deployable)</description>

    <dependencies>
        <dependency>
            <groupId>com.prenotazioni</groupId>
            <artifactId>shared</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- TestJwt: firma token di prova con lo stesso segreto, cosi' i test di questo
             servizio non hanno bisogno che auth-service giri. E' la conseguenza pratica
             del fatto che la validazione JWT sia offline. -->
        <dependency>
            <groupId>com.prenotazioni</groupId>
            <artifactId>shared</artifactId>
            <version>${project.version}</version>
            <type>test-jar</type>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
        <!-- Migrazioni versionate dello schema. Prima lo schema reale era quello
             generato da ddl-auto=update e i file .sql del progetto non venivano
             mai eseguiti, con la conseguenza che i vincoli di dominio mancavano. -->
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-devtools</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <!-- Messaggistica: la notifica di cancellazione viaggia su coda invece che via
             REST, cosi' sopravvive a notifica-service spento. Versione gestita da Boot. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-amqp</artifactId>
        </dependency>

        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>${jjwt.version}</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${lombok.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-logging</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
        </dependency>

        <!-- Test dependencies -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>test</scope>
        </dependency>
        <!--
            Testcontainers: una sola classe di test gira su PostgreSQL reale, per coprire
            cio' che H2 non sa esprimere (EXCLUDE USING gist, CHECK constraint, migrazioni
            Flyway). Le altre 9 classi di integrazione restano su H2, veloci.

            Nessun <version>: la 1.19.8 e' gia' gestita da spring-boot-dependencies.

            junit:junit (JUnit 4) arriva transitivo e NON va escluso: PostgreSQLContainer
            eredita da una gerarchia che implementa org.junit.rules.TestRule, quindi serve
            a compile-time anche se i test sono tutti su JUnit 5.
        -->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>postgresql</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Il broker nei test e' un container vero e NON e' opzionale: la classe che lo usa
             deve FALLIRE se Docker manca, non essere saltata. Un test sulla messaggistica
             che si auto-disabilita non prova nulla, ed e' gia' costato quattro asserzioni
             nascoste altrove in questo progetto. -->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>rabbitmq</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <!-- L'aggregatore disattiva il plugin per non far fallire
                         "spring-boot:run -pl app -am": qui va riacceso. -->
                    <skip>false</skip>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <!-- Solo la classe @SpringBootApplication: il main() non e' testabile
                             in modo significativo. Volutamente NON si escludono i dto: quelle
                             righe sono gia' quasi tutte coperte (Jackson invoca i getter
                             serializzando), quindi escluderle abbasserebbe la percentuale. -->
                        <exclude>com/prenotazioni/prenotazione/PrenotazioniAuleBackendApplication.class</exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
